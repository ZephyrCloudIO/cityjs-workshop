export function App() {
  return (
    <main className="app-shell">
      <span className="eyebrow">Turbo / Rsbuild</span>
      <h1>Rsbuild app ready in the monorepo.</h1>
      <p>Rspack-powered build; separate workspace; same React baseline.</p>
    </main>
  );
}
